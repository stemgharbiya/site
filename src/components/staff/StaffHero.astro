---
import { Icon } from "astro-icon/components";
import { ICONS } from "../../data/icons";
---

<section class="relative overflow-hidden bg-gradient-to-b from-primary/10 via-background to-background py-5 md:py-8 min-h-[60vh] flex items-center">
  <div class="absolute -top-32 right-0 h-72 w-72 rounded-full bg-primary/10 blur-3xl" aria-hidden="true"></div>
  <div class="absolute -bottom-32 left-0 h-72 w-72 rounded-full bg-secondary/60 blur-3xl" aria-hidden="true"></div>

  <div class="section-inner relative">
    <div class="max-w-3xl">
      <p class="eyebrow">Our Team</p>
      <h1 class="section-title mt-3 text-4xl md:text-5xl lg:text-6xl">
        Meet the dedicated educators shaping tomorrow's innovators.
      </h1>
      <p class="section-lead max-w-2xl">
        Our faculty and staff bring expertise, passion, and commitment to creating an exceptional 
        STEM learning environment. From experienced teachers to support staff, each member plays 
        a vital role in our students' success.
      </p>

      <div class="mt-10 grid gap-6 sm:grid-cols-3">
        <div class="surface p-5">
          <div class="flex items-center gap-2 text-sm font-medium text-primary">
            <Icon name={ICONS.faculty} class="h-5 w-5" aria-hidden="true" />
            Expert Educators
          </div>
          <p class="mt-2 text-xs text-muted-foreground">Qualified and passionate STEM teachers</p>
        </div>
        <div class="surface p-5">
          <div class="flex items-center gap-2 text-sm font-medium text-primary">
            <Icon name={ICONS.mentorship} class="h-5 w-5" aria-hidden="true" />
            Student Support
          </div>
          <p class="mt-2 text-xs text-muted-foreground">Dedicated to student growth and success</p>
        </div>
        <div class="surface p-5">
          <div class="flex items-center gap-2 text-sm font-medium text-primary">
            <Icon name={ICONS.handshake} class="h-5 w-5" aria-hidden="true" />
            Collaborative Teams
          </div>
          <p class="mt-2 text-xs text-muted-foreground">Working together across committees</p>
        </div>
      </div>
    </div>
  </div>
</section>
